
export const setUser =(user)=>({
    
    type:"SET_USER",
    user

})

export const setPublicFiles =(public_files)=>({
    
        type:"SET_PUBLIC",
        public_files
    
})

export const addFile = (file)=>({
    type:"ADD_FILE",
    file
})